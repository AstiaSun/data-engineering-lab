## How to run:

1. Setup and run `hw8` according to instructions inside the package
2. In terminal, change the current directory to `hw9`
3. Run script `run.sh`

After the container start, the files with tweets should be created in `hw9/.volumes/consumer/storage`.
