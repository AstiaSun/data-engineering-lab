### Instructions:

1. Open terminal and change the current working directory to `hw3`
2. Create a .env file from .env.template
3. Run bash script:
    ```commandline
    $ chmod +x run.sh && ./run.sh
    ```