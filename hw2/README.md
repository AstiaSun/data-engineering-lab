### Instructions:

1. Open terminal and change your current directory to `hw2`
2. Create `.env` file from `.env.template` and specify all env variables 
3. Run bash script:
    ```commandline
    $ chmod +x run.sh && ./run.sh
    ```
