### How to run:

1. Place CSV file to dataset folder: `hw6/dataset/amazon_reviews.csv`
2. Open terminal and change the current working directory to `hw6`
3. Create an `.env` file from `.env.template` 
4. Run bash script:
    ```commandline
    $ chmod +x run.sh && ./run.sh
    ```