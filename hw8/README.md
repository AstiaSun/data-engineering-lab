### Instructions:

1. Open terminal and change your current directory to `hw8`
2. Create a file `producer.env` from `producer.env.template` and set environment variables
3. Run bash script:
    ```commandline
    $ chmod +x run.sh && ./run.sh
    ```
