### Instructions:

1. Copy CSV file to dataset folder: `hw8/dataset`
2. Open terminal and change your current directory to `hw8`
3. Run bash script:
    ```commandline
    $ chmod +x run.sh && ./run.sh
    ```
