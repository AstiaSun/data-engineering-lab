networks:
  hw8-tier:
    driver: bridge

services:
  kafka:
    image: bitnami/kafka:4.0.0
    networks:
      - hw8-tier
    ports:
      - '9092:9092'
    environment:
      - KAFKA_CFG_NODE_ID=0
      - KAFKA_CFG_PROCESS_ROLES=controller,broker
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=0@kafka:9093
      - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
    volumes:
      - .volumes/kafka-persistence:/bitnami/kafka
    healthcheck:
      test: /kafka/bin/kafka-topics.sh --list --bootstrap-server localhost:9092 || exit 1
      interval: 1s
      timeout: 60s
      retries: 60

  producer:
    build: .
    networks:
      - hw8-tier
    volumes:
      - ./dataset:/data-engineering-lab/dataset:ro
      - ./.volumes/producer-logs:/data-engineering-lab/.logs/
    env_file: producer.env
    environment:
      - KAFKA_HOST=kafka
    command: bash -c "poetry run python -m src.hw8.main"