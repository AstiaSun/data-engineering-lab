### How to run:

1. Run hw3 following the instruction in the directory
2. Open terminal and change the current working directory to `hw5`
3. Duplicate `.env` file from hw3 to hw5
4. Run bash script:
    ```commandline
    $ chmod +x run.sh && ./run.sh
    ```